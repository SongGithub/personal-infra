#!/usr/bin/env bash

set -ex

die() { printf '%s\n' "$*" >&2; exit 1; }

curl http://localhost

resp=$(curl --write-out %{http_code} --silent --output /dev/null http://localhost)


[ "$resp" -eq 200 ] || die "there has been an error with web service!"
